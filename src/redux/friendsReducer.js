let initialState = {
    friendsData: [
        { id: 1, name: 'Kris' },
        { id: 2, name: 'Dima' },
        { id: 3, name: 'Vital' },
        { id: 4, name: 'Zhenkaf' }
      ]
};

const friendsReducer = (state = initialState, action) => {

    return state;
}
export default friendsReducer;